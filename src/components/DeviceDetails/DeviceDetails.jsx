import React from 'react';


const DeviceDetails = (props) => {
    return (
        <div>
            <p>its about : {props.price}</p>
            
        </div>
    );
};

export default DeviceDetails;